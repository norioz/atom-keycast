'use babel';

import GeneratedPackageView from '../lib/generated-package-view';

describe('GeneratedPackageView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
